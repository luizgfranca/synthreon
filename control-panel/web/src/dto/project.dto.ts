export type ProjectDto = {
    id: number,
    acronym: string,
    name: string,
    description: string
}

export type NewProjectDto = {
    acronym: string,
    name: string,
    description: string
}